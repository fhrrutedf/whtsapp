import { TeamSettings } from '@/components/settings/TeamSettings';

export const metadata = {
  title: 'فريق العمل والتوجيه الذكي | OmniDesk',
  description: 'إدارة الأعضاء والصلاحيات وقواعد التحويل الذكي للأقسام',
};

export default function TeamSettingsPage() {
  return <TeamSettings />;
}
